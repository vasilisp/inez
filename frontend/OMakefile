open build/OCaml

BYTE_ENABLED = false
NATIVE_ENABLED = true
OCAMLFLAGS += -thread
USE_OCAMLFIND = true
OCAMLPACKS[] += core

OCamlProgram(ibct, smtlib_parser smtlib_lexer main)

.PHONY: clean

clean:
    rm -f *.o *.cm[ix] smtlib_lexer.ml ibct ibct.opt *.omc
